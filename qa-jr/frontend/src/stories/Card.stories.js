import React from "react";
import { Card, CardTitle, CardForm } from "../components";
export default {
  title: "Cards"
};

export const CardContainer = () => (
  <Card>
    <CardTitle>CardTitle</CardTitle>
    <CardForm>a</CardForm>
  </Card>
);
