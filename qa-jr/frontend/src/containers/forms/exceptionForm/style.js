import styled from "styled-components";
export const ExceptionCreation = styled.div(props => ({
    display: "flex",
    flexDirection: "column",
    height: "100%",
    justifyContent: "space-around",
    marginTop: "50px",
    ...props,
}))